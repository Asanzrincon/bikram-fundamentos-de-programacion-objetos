describe("Objetos", () => {

});

describe("Objetos y arrays", () => {
    describe("Declaración", () => {
        describe("1.- Crea un 'objeto' de nombre 'Coche' que tengas las propiedades: 'marca, modelo, matricula'", () => {
            it("Contiene la propiedad marca", () => {
                expect(Coche.hasOwnProperty('marca'))
            })
        })
    });

    describe("Lectura de propiedades", () => {

    });

    describe("Modificación de propiedades", () => {

    });
});